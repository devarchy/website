const load_view = require('./view');
const load_events = require('./events');
const load_things = require('./things');

Object.assign(module.exports, {
    load_view,
    load_events,
    load_things,
});
